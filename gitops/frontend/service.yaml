apiVersion: v1
kind: Service
metadata:
  labels:
    app: frontend-ui
  name: frontend-ui-service
  namespace: frontend-ui
spec:
  ports:
  - name: "80"
    port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: frontend-ui
  type: ClusterIP
status:
  loadBalancer: {}